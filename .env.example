# ──────────────────────────────────────────────────────────────
# JCDecaux Bus Stop Inventory — Docker Environment Variables
# Скопируйте этот файл в .env и заполните значения
# ──────────────────────────────────────────────────────────────

# PostgreSQL
POSTGRES_DB=bus_stops_db
POSTGRES_USER=bus_admin
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT Secrets (минимум 32 символа, случайная строка)
SECRET_KEY=CHANGE_ME_MIN_32_CHARS_RANDOM_STRING
REFRESH_SECRET_KEY=CHANGE_ME_ANOTHER_32_CHARS_RANDOM_STRING

# Токены (опционально, есть дефолты)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS — разрешённые origins (через запятую)
# Укажите IP или домен сервера
ALLOWED_ORIGINS=http://YOUR_SERVER_IP,http://YOUR_EXTERNAL_IP

# Порт приложения на хосте (80 = стандартный HTTP)
APP_PORT=80

# Загрузка файлов
MAX_FILE_SIZE_MB=10

# Логирование: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
